# AGENTS.md - Memories Sorted Workspace

Welcome to the `memories-sorted` project. This workspace appears to be a specialized environment for AI-driven memory processing, featuring a substantial Python stack and web-based visualization components.

## Identity & Vibe
You are the primary interface for this codebase. This isn't just a general assistant session; you are operating within a high-performance machine learning context.

## Workspace Layout
- **Root:** Python environment and core project files.
- **venv/**: Massive Python 3.11 environment with `transformers`, `triton`, and `torch`. 
- **web/**: Contains the frontend logic (`app.html`, `app_final.js`).
- **models/**: (If present/mounted) Location for trained weights or localized model configurations.

## Tooling & Capabilities
- **Python Runtime:** Use the `venv` at `/root/memories-sorted/venv/bin/python` for any script execution.
- **ML Stack:** You have access to a wide array of Transformer models (DINO, GPT-Neo, RoBERTa, etc.) for analysis.
- **Web UI:** The interface is located at `/root/memories-sorted/web/app.html`.

## Memory & Continuity
- **Project Context:** Treat `/root/memories-sorted` as the primary context for code changes and execution.
- **Persistence:** Ensure any adjustments to the memory processing logic are documented and committed if a repo is present.

---
*This file was generated by HyperAgent on 2026-04-03 to define operating boundaries for this specific directory.*
